<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Gallery</title>
<style>
.gallery { max-width:900px; margin:40px auto; text-align:center; }
.preview-container { position:relative; height:500px; }
.preview-container img { position:absolute; top:0; left:0; width:100%; height:100%; object-fit:contain; opacity:0; transition:0.3s; z-index:0; }
.preview-container img:first-child { opacity:1; z-index:1; }
.thumbnail-container { display:flex; justify-content:center; gap:10px; margin-top:15px; }
.thumbnail-container img { width:100px; height:70px; object-fit:cover; cursor:pointer; opacity:0.7; transition:0.2s; }
.thumbnail-container img:hover { opacity:1; outline:2px solid rgba(255,255,255,0.7); }
.thumbnail-container img.active { outline:2px solid #fff; opacity:1; }
.nav-btn { position:absolute; top:50%; transform:translateY(-50%); background:rgba(255,255,255,0.2); border:none; color:#fff; font-size:30px; padding:10px; cursor:pointer; }
.nav-btn:hover { background:rgba(255,255,255,0.4); }
#prev { left:0; }
#next { right:0; }
</style>
</head>
<body>

<div class="gallery">
  <div class="preview-container">
    <button id="prev" class="nav-btn">&#10094;</button>
    <img id="preview" src="photo1.jpg">
    <button id="next" class="nav-btn">&#10095;</button>
  </div>

  <div class="thumbnail-container" id="thumbnails">
    <img src="photo1.jpg" class="active">
    <img src="photo2.jpg">
    <img src="photo3.jpg">
  </div>
</div>

<script>
const preview=document.getElementById("preview");
const thumbnails=document.querySelectorAll("#thumbnails img");
const prevBtn=document.getElementById("prev");
const nextBtn=document.getElementById("next");
let current=0;
function showImage(index){
  current=index;
  preview.src=thumbnails[index].src;
  thumbnails.forEach(t=>t.classList.remove("active"));
  thumbnails[index].classList.add("active");
}
prevBtn.onclick=()=>{ current=(current-1+thumbnails.length)%thumbnails.length; showImage(current); };
nextBtn.onclick=()=>{ current=(current+1)%thumbnails.length; showImage(current); };
thumbnails.forEach((thumb,i)=>{ thumb.onclick=()=>showImage(i); });
</script>

</body>
</html>
